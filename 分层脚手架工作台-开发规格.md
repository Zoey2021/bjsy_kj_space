# 任务：开发「分层脚手架工作台」功能

你是一名熟悉 Vue 3 教育类应用的工程师。请为本平台新增「分层脚手架工作台」模块，实现 A/B/C 三档学生的差异化任务分发。以下是完整的背景、现有代码结构和功能规格。

---

## 一、项目背景

平台是「杭州市滨江实验小学 · 信息科技学习空间」，教师用它给小学生（当前试点：六年级 2021 级）上课。现有一个前测模块，按学生答题结果把学生分为三档：

- **A 档**：不仅完成基础任务，还能优化、改造算法，解决开放性变式问题
- **B 档**：全部完成课标基础任务；模仿迁移解决同类简单问题（课堂达成基准）
- **C 档**：能看懂别人的算法实例，会识别顺序/分支/循环三种基本结构，完成观察、识别即可，不要求独立设计算法

本任务的目标：让教师在每节课的每个学习活动上，为三档学生配置不同难度的任务与提示（脚手架），学生端**按档自动路由**内容，且**学生永远看不到自己的档位标签**（隐形分层）。

## 二、技术栈与现有代码（不要重复造轮子）

- 前端：Vue 3（Composition API + `<script setup>`）+ Vite + Element Plus，路由懒加载，scoped CSS
- 后端：通过 `/api/` 前缀反向代理，前端从 localStorage 取 token 放 Authorization header
- 已有模块，直接复用：
  1. **前测与分档**：已有 `pretest/g6` 前测，教师端 `TeacherPretestG6` 页面已实现 A/B/C 分类（依据总分 + 操作关键项得分，满分 100），学生档案中已有档位字段。**直接读取该档位字段，不要重新实现分档逻辑**
  2. **课时活动结构**：`LessonDetail` 页面用 `step` 和 `activityIndex` 两个 URL 参数控制进度，活动间有前置锁定（step-lock），完成活动自动加 2 积分并提交记录
  3. **课时数据结构**：课时 = 单元 → 课时 → 多个学习活动（activities 数组）
  4. **教师端课程编辑器**：已有 `course-editor` 和 `activity-editor` 路由
  5. **教师端班级学情看板**：已有 SSE 实时推送（`/api/dashboard/sse/${classId}`）
- **不引入新的第三方依赖**，Element Plus 现有组件够用

## 三、数据模型

新增两张表 + 一张扩展：

```js
// 1. 脚手架模板 scaffold_templates（可复用的档位定义模板）
{
  id: string,
  name: string,            // 如「六年级算法单元通用模板」
  tierA: { goal: string, criteria: [string] },  // A档目标与达成标准
  tierB: { goal: string, criteria: [string] },
  tierC: { goal: string, criteria: [string] },
  createdAt: timestamp
}

// 2. 课时脚手架 lesson_scaffolds（每课时一份，含三档内容）
{
  id: string,
  lessonId: string,        // 关联现有课时
  templateId: string|null, // 使用的模板
  status: 'draft' | 'published',   // 只有 published 才对学生生效
  items: [                 // 与课时 activities 一一对应
    {
      activityIndex: number,
      tiers: {
        A: {
          task: string,          // 开放变式挑战任务描述
          hints: [string],       // 递减提示（至少2层，最多4层）
          challenge: string,     // 变式/拓展问题
        },
        B: {
          task: string,          // 基础任务描述
          hints: [string],       // 提示递减
          example: string,       // 半成品示例（部分完成）
        },
        C: {
          task: string,          // 观察识别任务描述
          hints: [string],
          workedExample: string, // 完整示例算法
          traceQuestions: [      // tracing 题：预测输出/找茬/填空
            { question: string, options: [string], answer: number }
          ],
        }
      }
    }
  ]
}

// 3. 学生表扩展：已有 tier 字段（前测产出）。新增教师手动覆盖字段
//    tierOverride: 'A'|'B'|'C'|null，取值优先级：tierOverride > tier > 默认 'B'
```

## 四、功能模块规格

### M1 教师端：分层脚手架工作台（新增路由 /teacher/scaffold-workbench）

页面结构（三步向导式，顶部步骤条）：

**第一步：选择课时与模板**
- 左侧：从课程树选择课时（复用现有 course-editor 的课程树组件）
- 右侧：选择脚手架模板（下拉，来自 scaffold_templates），或直接编辑三档定义
- 显示该课时已有的 activities 列表（只读），每个活动一行

**第二步：AI 生成三档草稿**
- 每个活动行有一个「AI 生成」按钮，点击后后端调用 LLM 生成三档内容草稿，填充到该行的三档编辑区
- 支持整课时批量生成（一个按钮生成所有活动的草稿）
- 生成过程中显示 loading，可取消
- 三档内容以 Tab 切换展示（A 档 / B 档 / C 档），每个字段可直接编辑

**第三步：教师审核与发布**
- 每个活动一行，默认未勾选；教师逐条审核，可编辑后勾选「已审核」
- 全部勾选后「发布」按钮才可用；发布后 status=published
- 已发布的课时支持「重新编辑」，重新编辑后回到 draft，需再次发布

### M2 AI 生成接口（后端）

- 新增 `POST /api/teacher/scaffold/generate`
- 入参：`{ lessonId, activityIndex | 'all', tierDefinitions: {A,B,C 的 goal+criteria} }`
- 后端读取该课时的活动内容（活动标题、知识点），构造提示词调用 LLM，返回结构化 JSON（三档的 task/hints/example/traceQuestions）
- LLM 调用要求：
  - 使用环境变量配置：`AI_BASE_URL`、`AI_API_KEY`、`AI_MODEL`（兼容 OpenAI chat/completions 格式，SiliconFlow 等均可）。**密钥只能放后端环境变量，严禁出现在前端代码**
  - 提示词中明确要求：面向小学六年级、语言口语化、C 档 tracing 题答案必须唯一且正确、hints 按提示强度递减排列
  - LLM 返回必须是可解析的 JSON；解析失败时重试一次，仍失败返回明确错误给前端提示「AI 生成失败，请手动填写或重试」

### M3 学生端：按档路由（隐形分层）

- `LessonDetail` 加载课时数据时，新增请求 `GET /api/learn/lesson-content?lessonId=xxx`
- 后端逻辑：查学生档位（tierOverride > tier > 'B'），若该课时脚手架 status=published，返回对应档位的任务内容；否则返回原有活动内容（向后兼容，未配置脚手架的课时行为完全不变）
- **安全红线：该接口的响应中绝不包含档位字段、绝不包含其他档位的内容**，前端任何地方不显示"A/B/C"字样
- 活动页面的改动最小化：复用现有活动渲染结构，把任务描述、示例区、提示按钮替换为档位内容：
  - 提示按钮：点击逐层展开 hints（第一次点击显示第 1 层，再点显示第 2 层…），并上报提示使用次数
  - C 档额外渲染 traceQuestions（选择题形式，复用现有小测的组件与提交逻辑）
- 活动完成判定与积分逻辑不变：三档学生完成各自任务都正常加 2 积分，**评分标准按档内达成标准**（C 档完成识别任务即达标）

### M4 教师端：档位管理（扩展 TeacherPretestG6 或新增小页）

- 展示班级学生名单 + 当前档位（A/B/C 计数统计 + 名单）
- 教师可手动调整单个学生的 tierOverride（下拉选择，带二次确认弹窗：「调整后学生将收到不同难度的任务，确定？」）
- 可选（P2，本期可不做）：根据过程数据（小测得分、提示使用次数）给出档位调整建议，只提示不自动改

### M5 数据看板扩展（教师端班级学情）

- 看板按活动维度增加显示：各档人数分布、各档完成率、各档平均提示使用次数
- 注意：只在教师端显示档位维度，学生端永远不可见

## 五、API 清单汇总

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/teacher/scaffold/templates | 模板列表 |
| POST | /api/teacher/scaffold/templates | 新建/保存模板 |
| GET | /api/teacher/scaffold/:lessonId | 读取某课时的脚手架（教师端，含全部档位内容） |
| POST | /api/teacher/scaffold/generate | AI 生成三档草稿 |
| POST | /api/teacher/scaffold/:lessonId/publish | 发布（入参：审核通过的 items） |
| GET | /api/learn/lesson-content?lessonId= | 学生端按档取内容（不含档位信息） |
| POST | /api/learn/hint-used | 上报提示使用次数 |
| GET | /api/teacher/tiers?classId= | 班级档位名单与统计 |
| POST | /api/teacher/tiers/override | 教师调整学生档位 |

## 六、交付要求

1. 遵循现有代码风格：Vue 3 `<script setup>`、Element Plus、scoped CSS、路由懒加载（新页面拆独立 chunk）
2. 所有新接口走 `/api/` 前缀，鉴权方式与现有接口一致
3. 未配置脚手架的课时行为与现在完全一致（向后兼容是硬要求）
4. **隐形分层是硬要求**：学生端接口与 UI 全链路不出现档位信息，写代码时自查一遍
5. AI 密钥仅存后端环境变量；生成失败要有明确的用户提示与重试路径
6. 数据库迁移脚本一并给出（新增两张表 + 学生表一个字段）
7. 先给出实现方案（涉及文件清单 + 改动点），确认后再写代码
